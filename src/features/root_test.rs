#[cfg(test)]
mod test {

    #[test]
    fn root() {
        /*
        let events = [].iter();
        let sender = ReportCollector::default();
        let root_opt = init(events, &mut sender).unwrap();
        let root = root_opt.unwrap();

        assert_eq!(sender.reports, vec![HidppReport::default()]);
        */
    }
}
